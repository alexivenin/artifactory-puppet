####THIS FILE IS MANAGED BY PUPPET, PLEASE DO NOT EDIT ####

data_directory = '<%=@data_directory%>'		# use data in another directory
hba_file = '<%=@hba_file%>'	# host-based authentication file
ident_file = '<%=@ident_file%>'	# ident configuration file

# If external_pid_file is not explicitly set, no extra PID file is written.
external_pid_file = '<%=@external_pid_file%>'		# write an extra PID file
listen_addresses = '<%=@listen_addresses%>'		# what IP address(es) to listen on;

port = <%=@pg_port%>				# (change requires restart)
max_connections = <%=@max_connections%>			# (change requires restart)
unix_socket_directories = '/var/run/postgresql'	# comma-separated list of directories
ssl = <%=@ssl%># (change requires restart)
ssl_cert_file = '/etc/ssl/certs/ssl-cert-snakeoil.pem'		# (change requires restart)
ssl_key_file = '/etc/ssl/private/ssl-cert-snakeoil.key'		# (change requires restart)
shared_buffers = <%=@shared_buffers%>			# min 128kB
log_line_prefix = '%t '			# special values:
datestyle = 'iso, mdy'
timezone = 'UTC'

lc_messages = '<%=@locale%>'			# locale for system error message
lc_monetary = '<%=@locale%>'			# locale for monetary formatting
lc_numeric = '<%=@locale%>'			# locale for number formatting
lc_time = '<%=@locale%>'				# locale for time formatting

# default configuration for text search
default_text_search_config = 'pg_catalog.english'
